import 'package:flutter/material.dart';

// === BRAND COLORS ===

// Primary Brand Color (Used for CTAs like "Add to Cart", "Buy Now", etc.)
Color brandPrimaryColor = const Color(0xFF1A73E8); // Vibrant Blue

// Secondary Accent Color (Used in navigation bars, buttons, headers)
Color brandSecondaryColor = const Color(
  0xFFFFB300,
); // Bright Amber (like Amazon's gold)

// === STATUS COLORS ===

// Success / Confirmed Order / Payment Success
Color statusSuccessColor = const Color(0xFF34A853); // Green (Google style)

// Error / Out of Stock / Payment Failed
Color statusErrorColor = const Color(0xFFEA4335); // Red

// Warning / Low Stock
Color statusWarningColor = const Color(0xFFFFC107); // Amber

// Info / For soft highlights
Color statusInfoColor = const Color(0xFF17A2B8); // Teal Blue

// === BACKGROUND & SURFACE ===

// General App Background
Color backgroundColor = const Color(0xFFF8F9FA); // Light neutral background

// Card Backgrounds / Product Tiles
Color cardBackgroundColor = const Color(0xFFFFFFFF); // Pure white

// Section Headers / Footers
Color surfaceColor = const Color(0xFFF1F3F4); // Soft grey

// === TEXT & ICONS ===

// Main Text / Headlines
Color textPrimaryColor = const Color(0xFF202124); // Deep grey/black

// Secondary Text / Captions
Color textSecondaryColor = const Color(0xFF5F6368); // Medium grey

// Disabled Text / Icons
Color textMutedColor = const Color(0xFF9AA0A6); // Light grey

// Highlighted text / Price tags / Links
Color textHighlightColor = const Color(0xFF1A73E8); // Matches brandPrimaryColor

// === BORDER & DIVIDERS ===

// Light Dividers / Card borders
Color borderColor = const Color(0xFFE0E0E0);

// Focus Outline / Active elements
Color focusColor = const Color(0xFF2962FF);

// === ADDITIONAL UTILITIES ===

// Hover Background
Color hoverBackgroundColor = const Color(0xFFE8F0FE);

// Selected Item Background (like selected filter)
Color selectedItemBackground = const Color(0xFFD2E3FC);

// Sale Banner / Flash Deal Badge
Color saleBadgeColor = const Color(0xFFFF5252); // Flashy Red

// Discount Label Background
Color discountLabelColor = const Color(0xFF00C853); // Flash Green

const Color neutralWhiteColor = Color(
  0xFFFFFFFF,
); // White text (for buttons, etc.)
const Color darkColor = Color(0xFF121212);
